object Q1{
    def main(args: Array[String]): Unit = {

        var i, j, k: Int = 2
        var m, n: Int = 5
        var f, g: Float = 12.0f
        var c: Char = 'X'

        println(s"a). ${k + 12 * m}")
        println(s"b). ${m / n}")
        println(s"c). ${n%j}")
        println(s"d). ${m / j * j}")
        println(s"e). ${f + 10 * 5 + g}")

        i += 1
        println(s"f). ${i * n}")
    }
}

